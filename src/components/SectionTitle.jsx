const SectionTitle = ({title}) => {
    return ( 
        <div>
            <h2 className=" bg-custom-white-gradient bg-clip-text text-transparent mb-6 font-bold text-[54px] leading-[64.8px] text-center">{title}</h2>
        </div>
     );
}
 
export default SectionTitle; 