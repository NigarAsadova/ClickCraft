const SectionTitle = ({title}) => {
    return ( 
        <div className="flex justify-center">
            <h2 className=" bg-custom-white-gradient bg-clip-text text-transparent mb-6 font-bold text-[54px] leading-[64.8px] text-center w-fit  max-lg:text-4xl max-md:text-2xl">{title}</h2>
        </div>
     );
}
 
export default SectionTitle; 